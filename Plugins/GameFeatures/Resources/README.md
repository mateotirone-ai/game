# Resources

Game Feature plugin skeleton for **Resources**.

- Author `GF_Resources` GameFeatureData in-editor under this plugin's `Content/`.
- Implement domain logic here or in a future runtime module under `Source/`.
- Spec: see `Design/` and `Plugins/GameFeatures/README.md`.
- Disabled by default until content exists (`EnabledByDefault: false`).
